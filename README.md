# text-translit-flask
Это клиент-серверное приложение на Python и Flask для перекодировки русского текста в транслит. Поддерживает загрузку файлов, drag-and-drop, генерацию результата на сервере и ведение журнала запросов в SQLite через SQLAlchemy.
